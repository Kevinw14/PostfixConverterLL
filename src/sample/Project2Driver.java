package sample;

import javafx.application.Application;

/**
 * Postfix Converter application that takes an infix expression from
 * the user and converts it to a postfix expression and shows it to
 * the user.
 *
 * @author Kevin Wood
 * @version 1.0
 */
public class Project2Driver {
    public static void main(String[] args) {
        Application.launch(ConverterView.class, args);
    }
}
